import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Boosmap-Registro-Viajes',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
